wifi:
  ssid: # <SSID>
  pass: # <password>
mqtt:
  host: # <MQTT Host>
# port: # <Optional Port>
# user: # <Optional user>
# pass: # <Optional password>
#gpiox: # Optional GPIO Extender
# num:  # Number of GPIO extenders 0 - 4
# sda:  # Pin used for I2C SDA
# scl:  # Pin used for I2C SCL  
thing:
  profile: |
        my_switch = toggleSwitch(14)
        switchedRelay(13, 1, my_switch)